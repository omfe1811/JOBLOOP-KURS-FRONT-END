Task:
Return the string received as the first parameter with the following changes:

Each letter in the string that matches charA (second parameter) should be replaced with charB (third parameter), and vice versa. This means that letters matching charA should be swapped with charB, and letters matching charB should be swapped with charA.